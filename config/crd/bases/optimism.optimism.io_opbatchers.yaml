---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: opbatchers.optimism.optimism.io
spec:
  group: optimism.optimism.io
  names:
    kind: OpBatcher
    listKind: OpBatcherList
    plural: opbatchers
    singular: opbatcher
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.optimismNetworkRef.name
      name: Network
      type: string
    - jsonPath: .spec.sequencerRef.name
      name: Sequencer
      type: string
    - jsonPath: .status.batcherInfo.pendingBatches
      name: Pending Batches
      type: integer
    - jsonPath: .status.batcherInfo.totalBatchesSubmitted
      name: Total Batches
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpBatcher is the Schema for the opbatchers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpBatcherSpec defines the desired state of OpBatcher.
            properties:
              batching:
                description: Batching Configuration
                properties:
                  approxComprRatio:
                    default: "0.4"
                    description: Approximate compression ratio (between 0.1 and 1.0,
                      as string)
                    type: string
                  maxChannelDuration:
                    default: 10m
                    description: Maximum duration for a channel
                    type: string
                  subSafetyMargin:
                    default: 10
                    description: Safety margin for L1 confirmations
                    format: int32
                    minimum: 1
                    type: integer
                  targetL1TxSize:
                    default: 120000
                    description: Target size for L1 transactions (bytes)
                    format: int32
                    minimum: 1000
                    type: integer
                  targetNumFrames:
                    default: 1
                    description: Target number of frames per transaction
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              dataAvailability:
                description: Data Availability Configuration
                properties:
                  maxBlobsPerTx:
                    default: 6
                    description: Maximum blobs per transaction (EIP-4844)
                    format: int32
                    maximum: 6
                    minimum: 1
                    type: integer
                  type:
                    default: blobs
                    description: Type of data availability
                    enum:
                    - blobs
                    - calldata
                    type: string
                type: object
              l1Transaction:
                description: L1 Transaction Management
                properties:
                  feeLimitMultiplier:
                    default: 5
                    description: Fee limit multiplier for dynamic fees
                    format: int32
                    minimum: 1
                    type: integer
                  numConfirmations:
                    default: 10
                    description: Number of confirmations to wait
                    format: int32
                    minimum: 1
                    type: integer
                  resubmissionTimeout:
                    default: 48s
                    description: Timeout before resubmitting transaction
                    type: string
                  safeAbortNonceTooLowCount:
                    default: 3
                    description: Abort threshold for nonce too low errors
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              metrics:
                description: Metrics Configuration
                properties:
                  enabled:
                    type: boolean
                  path:
                    type: string
                  port:
                    format: int32
                    type: integer
                type: object
              optimismNetworkRef:
                description: Network Reference
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              privateKey:
                description: Private Key for L1 Transaction Signing
                properties:
                  generate:
                    type: boolean
                  secretRef:
                    description: SecretKeySelector selects a key of a Secret.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              resources:
                description: Resources
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              rpc:
                description: RPC Configuration
                properties:
                  cors:
                    description: CORSConfig defines CORS settings
                    properties:
                      methods:
                        items:
                          type: string
                        type: array
                      origins:
                        items:
                          type: string
                        type: array
                    type: object
                  enableAdmin:
                    type: boolean
                  enabled:
                    type: boolean
                  host:
                    type: string
                  port:
                    format: int32
                    type: integer
                type: object
              sequencerRef:
                description: L2 Sequencer Configuration
                properties:
                  name:
                    description: Name of the sequencer OpNode
                    type: string
                  namespace:
                    description: Namespace of the sequencer OpNode (optional, defaults
                      to same namespace)
                    type: string
                required:
                - name
                type: object
              throttling:
                description: Throttling Configuration
                properties:
                  backlogSafetyMargin:
                    default: 10
                    description: Safety margin for backlog
                    format: int32
                    minimum: 1
                    type: integer
                  enabled:
                    default: true
                    description: Enable throttling
                    type: boolean
                  maxPendingTx:
                    default: 10
                    description: Maximum pending transactions
                    format: int32
                    minimum: 1
                    type: integer
                type: object
            required:
            - optimismNetworkRef
            - privateKey
            - sequencerRef
            type: object
          status:
            description: OpBatcherStatus defines the observed state of OpBatcher.
            properties:
              batcherInfo:
                description: BatcherInfo contains operational information about the
                  batcher
                properties:
                  lastBatchSubmitted:
                    description: LastBatchSubmitted contains information about the
                      last submitted batch
                    properties:
                      blockNumber:
                        description: BlockNumber is the L2 block number of the batch
                        format: int64
                        type: integer
                      gasUsed:
                        description: GasUsed is the gas used by the L1 transaction
                        format: int64
                        type: integer
                      timestamp:
                        description: Timestamp is when the batch was submitted
                        format: date-time
                        type: string
                      transactionHash:
                        description: TransactionHash is the L1 transaction hash
                        type: string
                    type: object
                  pendingBatches:
                    description: PendingBatches is the number of pending batches
                    format: int32
                    type: integer
                  totalBatchesSubmitted:
                    description: TotalBatchesSubmitted is the total number of batches
                      submitted
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the OpBatcher's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the OpBatcher
                enum:
                - Pending
                - Running
                - Error
                - Stopped
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
